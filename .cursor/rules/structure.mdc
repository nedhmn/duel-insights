---
description: Describes how the repo is structured
globs:
alwaysApply: false
---

## Project Structure

This project is a monorepo where the `./packages/` directory holds the following applications:

- `./packages/backend` is a python fastapi app
- `./packages/frontend` is a nextjs app
- `./packages/docs` is an astro starlight docs app
